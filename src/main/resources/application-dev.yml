spring:
  datasource:
    url: jdbc:mysql://localhost:3306/arclearn?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: Li!@#19991226
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update  # 自动创建表（开发环境用，生产环境改validate）
    show-sql: true
    properties:
      hibernate:
        format_sql: true # 格式化SQL
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0

jwt:
  secret: devJwtSecretKey12345678901234567890 # 生产环境需加密存储
  expiration: 3600000 # 过期时间（1小时，单位：毫秒）


